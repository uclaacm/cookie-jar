import React, { Component } from "react";

class Landing extends Component {
    render(){
        return(
            <div style={{paddingTop: '10%', paddingLeft: '40%'}}>This is the Landing Page</div>
        )
    }
}

export default Landing;